# frozen_string_literal: true

RSpec.describe Brew2nix do
  it "has a version number" do
    expect(Brew2nix::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
